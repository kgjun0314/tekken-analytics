spring:
  application:
    name: tekken-analytics

  datasource:
    url: jdbc:postgresql://localhost:5432/tekken
    username: postgres
    password: 4904

  jpa:
    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true
