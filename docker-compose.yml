services:
  postgres:
    image: postgres:17

    container_name: tekken-postgres

    environment:
      POSTGRES_DB: tekken
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 4904

    ports:
      - "5432:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: